[project]
name = "ad-automation-system"
version = "0.1.0"
description = "Advertising automation system integrating ChatGPT, DALL·E, and Meta Ads Manager"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
  "pydantic>=2.8.0",
  "pydantic-settings>=2.4.0",
  "openai>=1.50.0",
  "requests>=2.32.0"
]

[tool.pytest.ini_options]
pythonpath = ["."]
